SELECT sourceip, UNIQUECOUNT(username) as target_users 
FROM events 
WHERE qidname(qid) ILIKE '%login failed%' OR qidname(qid) ILIKE '%logon failure%' 
GROUP BY sourceip 
HAVING target_users >= 5 
LAST 30 MINUTES
